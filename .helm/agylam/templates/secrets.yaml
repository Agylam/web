{{- define "secret_app" -}}
{{- $ := index . 0 -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $.Values.global.project }}-secret
data:
  {{- range $k, $v := $.Values.SECRETS }}
  {{ $k }}: {{ $v | b64enc | quote }}
{{- end -}}
{{- end -}}

{{- include "secret_app" (list $) -}}