name: Dev

on:
  push:
    branches: [ "cicd-test" ]
    tags:
      - '*'

jobs:
  build:
    strategy:
      matrix:
        modules: [front, back]
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ vars.DK_USERNAME }}
          password: ${{ secrets.DK_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v4
        with:
          file: ${{ matrix.modules }}/docker/Dockerfile
          context: ${{ matrix.modules }}
          push: true
          tags: agylam/${{ matrix.modules }}:${{ github.ref_name }}

  deploy:
    runs-on: ubuntu-latest
    needs:
      - build
    steps:
      - uses: actions/checkout@v3
      - name: Copy .env to stand
        uses: garygrossgarten/github-action-scp@release
        with:
            local: .env
            remote: ~/stand/.end
            host: ${{ secrets.SSH_HOST }}
            username: ${{ secrets.SSH_USER }}
            privateKey: ${{ secrets.SSH_KEY }}
            passphrase: ${{ secrets.SSH_KEYPASS }}

      - name: Copy docker-compose.yml to stand
        uses: garygrossgarten/github-action-scp@release
        with:
            local: docker-compose.yml
            remote: ~/stand/docker-compose.yml
            host: ${{ secrets.SSH_HOST }}
            username: ${{ secrets.SSH_USER }}
            privateKey: ${{ secrets.SSH_KEY }}
            passphrase: ${{ secrets.SSH_KEYPASS }}

      - name: Restart stand
        uses: appleboy/ssh-action@v0.1.10
        with:
            host: ${{ secrets.SSH_HOST }}
            username: ${{ secrets.SSH_USER }}
            key: ${{ secrets.SSH_KEY }}
            passphrase: ${{ secrets.SSH_KEYPASS }}
            script: |
                cd stand && sed -i "s/BRANCH=.*/BRANCH=${{ github.ref_name }}/g" .env && docker-compose pull && docker-compose up -d