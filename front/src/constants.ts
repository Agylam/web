export const enum PagePath {
    home = "/",
    schedule = "/schedule",
    announcement = "/announcement",
}

export const enum Role {
    student = "student",
    teacher = "teacher",
    director = "director",
    admin = "admin",
}
